<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_car_logo"
        android:label="Car Wash"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TFG_CarWash"
        tools:targetApi="31">

        <!-- SplashActivity como primera pantalla -->
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Autenticación -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />

        <!-- Pantalla principal -->
        <activity android:name=".MainActivity" />

        <!-- Perfil -->
        <activity android:name=".ProfileActivity" />
        <activity android:name=".EditProfileActivity" />

        <!-- Nuevas pantallas añadidas -->
        <activity android:name=".BookingsActivity" />
        <activity android:name=".ServicesActivity" />
        <activity android:name=".ActivityHistory" />

        <!-- Detalle de servicios -->
        <activity android:name=".SelfServeActivity" />
        <activity android:name=".FullServiceActivity" />
        <activity android:name=".DeluxeWashActivity" />
        <activity android:name=".WaterlessWashActivity" />
        <activity android:name=".SteamWashActivity" />

    </application>

</manifest>
